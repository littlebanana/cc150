// Given a (decimal - e.g. 3.72) number that is passed in as a string, print the binary representation.
// If the number can not be represented accurately in binary, print "ERROR"

#include <iostream>
#include <string>

using namespace std;

class Solution
{
    public:
        void printBinary(string s)
        {
            // corner cases: negative sign, dot sign, overflow, underflow
        }
};

int main()
{
    return 0;
}
